#include <QObject>

/**
 * @brief Resets the staging cursor.
 */
void Sample::reset()
{
  //---------------------------------------------------------
  // For now, the cursor is reset here.
  //---------------------------------------------------------
  m_used = 0;
}
