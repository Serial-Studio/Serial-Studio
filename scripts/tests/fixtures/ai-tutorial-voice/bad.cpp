#include <QObject>

/**
 * @brief Resets the staging cursor.
 */
void Sample::reset()
{
  //---------------------------------------------------------
  // First, we reset the cursor.
  //---------------------------------------------------------
  m_used = 0;
}
