#include <QObject>

/**
 * @brief Resets the staging cursor.
 */
void Sample::reset()
{
  //---------------------------------------------------------
  // This method resets the cursor.
  //---------------------------------------------------------
  m_used = 0;
}
